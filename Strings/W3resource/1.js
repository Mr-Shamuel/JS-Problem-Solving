// 1. Write a program in C to input a string and print it. 
"use strict";
const prompt = require("prompt-sync")({sigint:true});
const data = prompt("Input the String : ");
console.log("The string you entered is : "+data);
